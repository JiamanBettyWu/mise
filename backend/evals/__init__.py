# Offline eval harness (#30/#85). Dev-only — nothing here is imported by the
# Render request path or the GitHub-cron jobs. `scorers` is deliberately free
# of weave imports so the pytest suite can cover it offline.
